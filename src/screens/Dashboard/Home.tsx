import React from "react";
import { Typography } from "@mui/material";

const DashboardHome: React.FC = () => (
  <Typography variant="h4">Bem-vindo ao Painel do Usuário</Typography>
);

export default DashboardHome;
